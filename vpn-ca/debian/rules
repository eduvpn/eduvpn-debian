#!/usr/bin/make -f

# See debhelper(7)
# output every command that modifies files on the build system.
#export DH_VERBOSE = 1

%:
	dh $@ --buildsystem=makefile --with=golang

# dh $@ --builddirectory=_build --buildsystem=golang --with=golang

